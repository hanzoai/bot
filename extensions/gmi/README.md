# Bot GMI Cloud Provider

Official Bot provider plugin for hosted GMI Cloud models through an
OpenAI-compatible API.

Install from Bot:

```bash
bot plugins install @hanzo/bot-gmi-provider
bot gateway restart
```

Configure a GMI Cloud API key, then select models with refs such as
`gmi/openai/gpt-5.6-sol`.
