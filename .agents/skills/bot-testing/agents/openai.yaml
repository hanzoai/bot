interface:
  display_name: "Bot Testing"
  short_description: "Choose cheap, targeted Bot validation"
  default_prompt: "Use $bot-testing to choose the cheapest safe test or CI verification path, inspect failures, and rerun only the relevant Bot lane."
