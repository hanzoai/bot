title: "Runtime tool fixture — session_status"

scenario:
  id: runtime-tool-session-status
  surface: runtime-tools
  runtimePairLane: core
  coverage:
    primary:
      - gateway.session-apis-session-status
  objective: Verify session_status preserves arguments and result shape across Bot and Codex.
  successCriteria:
    - Effective tools expose session_status.
    - The mock provider plans exactly one happy-path session_status call.
    - The mock provider plans one denied-input failure-path session_status call.
    - Runtime parity coverage hard-fails call/result drift in the core runtime-pair lane.
  docsRefs:
    - qa/scenarios/index.yaml
  codeRefs:
    - src/agents/tools/session-status-tool.ts
    - extensions/qa-lab/src/runtime-tool-fixture.ts
  execution:
    kind: flow
    summary: Exercise the session_status runtime tool family.
    config:
      toolName: session_status
      toolCoverage:
        family: session_status
        actualTool: session_status
        bucket: bot-dynamic-integration
        expectedLayer: bot-dynamic
        capabilityLayer: bot-dynamic-direct
        required: true
        codexDefaultImpact: P4
        qaImpact: P1
        action: hard gate in the core runtime-pair lane
        reason: session_status is an Bot integration tool and must stay visible and callable under Bot and Codex direct runtime parity.
      promptSnippet: "target=session_status"
      failurePromptSnippet: "failure target=session_status"

flow:
  steps:
    - name: exercises session_status happy and failure paths
      actions:
        - call: runRuntimeToolFixture
          saveAs: result
          args:
            - ref: env
            - ref: config
      detailsExpr: result
