<shortcuts xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <capability
        android:name="custom.actions.intent.ASK_BOT"
        app:queryPatterns="@array/ask_bot_query_patterns">
        <intent
            android:action="ai.bot.app.action.ASK_BOT"
            android:targetPackage="ai.bot.app"
            android:targetClass="ai.bot.app.MainActivity">
            <parameter
                android:name="prompt"
                android:key="prompt"
                android:mimeType="https://schema.org/Text"
                android:required="true" />
        </intent>
    </capability>
</shortcuts>
