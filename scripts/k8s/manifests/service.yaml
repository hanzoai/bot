apiVersion: v1
kind: Service
metadata:
  name: bot
  labels:
    app: bot
spec:
  type: ClusterIP
  selector:
    app: bot
  ports:
    - name: gateway
      port: 18789
      targetPort: 18789
      protocol: TCP
